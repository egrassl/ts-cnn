# Trains on UCF101 with all layers frozen except the last convolutional and dense ones

dataset: 'path to dataset'
classes: 'path to classes file'
annotations: 'path to annotations'
n_classes: 101
epochs: 300
batch_size: 32
train_only_last: True
learn_rate: 1e-3
l2_reg: 1e-4
dropout: 0.5
momentum: 0.9
weights: 'imagenet'
